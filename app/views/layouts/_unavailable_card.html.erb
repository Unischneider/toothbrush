<% @bookings.each do |booking| %>
<% if booking.ending_on > Date.today %>
<div class="col-xs-12 <%=  'col-sm-6' if params[:action] == "home" %> <%= 'col-sm-4' if params[:action] == "index" %>">
  <div class="card" style="background-image: linear-gradient(-225deg, rgba(55, 155, 255, 0.6) 0%, rgba(0, 36, 61, 0) 50%), url('<%= cl_image_path booking.toothbrush.photo %>');">

    <div class="hiddens">
      <div class="card-category">
        <%= booking.toothbrush.material %>
      </div>
      <div class="x-icon">
        <div class="unavailable-icon-index">
          <i style="z-index: -1;"  class="unavailable-icon fas fa-times-circle"></i>
        </div>
        <div class="dateis">
      <p>Available on: <br> <strong><%= booking.ending_on %></strong></p>
    </div>
      </div>
      <div class="card-description">
        <h2><%= link_to booking.toothbrush.name, toothbrush_path(booking.toothbrush.id) %>´s Toothbrush</h2>
        <p><%= booking.toothbrush.price %> EUROS</p>
      </div>
    </div>
  </div>
</div>
<% end %>
<% end %>
